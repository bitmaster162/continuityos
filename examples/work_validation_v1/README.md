# Work Validation Evidence v1 example

This directory shows the output shape produced by:

```text
continuity work-admission run-validation ...
```

Raw command outputs are binary evidence and are deliberately not included in the
source repository. The example JSON files use empty-output SHA-256 values only
as schema illustrations; they are not release evidence.
